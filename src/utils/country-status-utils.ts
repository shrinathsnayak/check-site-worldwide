// Re-export from new locations
export {
  getStatusColor,
  getStatusText,
  getCardBackgroundColor,
  getCardBorderColor,
  calculateCountryStats,
} from './component-utils';

export { groupCountriesByRegion } from './grouping-utils';

export type { CountryStatus } from '@/types/component-types';
